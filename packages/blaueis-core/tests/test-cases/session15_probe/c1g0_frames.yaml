# Real 0xC1 Group 0 frames — XtremeSaveBlue, Session 15
#
# Runtime timers: power-on time, total worked time, current session time.
# Field mapping cross-validated against community protocol research.
# All values zero in this capture — AC may need longer runtime to populate.

device: "XtremeSaveBlue"
session: "Session 15"
source: "ac_probe.py UART probe via raspi-midea gateway"
bus: "UART (body[1]=0x21)"
frame_type: "0xC1 Group 0"

frames:
  - name: "all zeros — timers not populated on this unit"
    timestamp: "2026-04-10T18:11:47Z"
    body_hex: "c1 21 01 40 00 00 00 00 00 00 00 00 00 00 00 00 00 00 00 00 00"
    ground_truth:
      # Three synthesised aggregates (composite/derived_from): each = days*86400 + hours*3600 + minutes*60 + seconds
      power_on_time: 0        # body[4..8]:   days(BE16) + h + m + s
      total_worked_time: 0    # body[9..13]:  days(BE16) + h + m + s
      current_session_time: 0 # body[14..18]: days(BE16) + h + m + s
    notes: "All zeros. Unit may not populate group 0 timers, or needs longer cumulative runtime."
